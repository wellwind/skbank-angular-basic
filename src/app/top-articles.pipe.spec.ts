import { TopArticlesPipe } from './top-articles.pipe';

describe('TopArticlesPipe', () => {
  it('create an instance', () => {
    const pipe = new TopArticlesPipe();
    expect(pipe).toBeTruthy();
  });
});
